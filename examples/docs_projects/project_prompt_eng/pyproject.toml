[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[tool.dg]
directory_type = "project"

[tool.dg.project]
root_module = "project_prompt_eng"
code_location_target_module = "project_prompt_eng.definitions"